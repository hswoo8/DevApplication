<?xml version="1.0" encoding="utf-8"?>
<MotionScene
        xmlns:motion="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android">

    <Transition
            motion:constraintSetStart="@id/start_constraint_set01"
            motion:constraintSetEnd="@id/end_constraint_set01"
            motion:duration="1000">
        <OnSwipe
                motion:touchAnchorId="@id/rectangle"
                motion:touchAnchorSide="right"
                motion:dragDirection="dragRight"/>

    </Transition>


    <ConstraintSet android:id="@+id/start_constraint_set01">
        <Constraint
                android:id="@id/rectangle"
                android:layout_width="64dp"
                android:layout_height="64dp"
                motion:layout_constraintLeft_toLeftOf="parent"
                motion:layout_constraintTop_toTopOf="parent">

            <CustomAttribute
                    motion:attributeName="BackgroundColor"
                    motion:customColorValue="@color/colorPrimary"/>
        </Constraint>
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end_constraint_set01">
        <Constraint
                android:id="@id/rectangle"
                android:layout_width="64dp"
                android:layout_height="64dp"
                motion:layout_constraintBottom_toBottomOf="parent"
                motion:layout_constraintRight_toRightOf="parent">
            <CustomAttribute
                    motion:customColorValue="@color/colorAccent"
                    motion:attributeName="BackgroundColor"/>
        </Constraint>
    </ConstraintSet>
</MotionScene>